{
  "dependencies": {
    "body-parser": "^1.15.2",
    "del": "^2.2.1",
    "express": "^4.14.0",
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.0"
  },
  "devDependencies": {
    "gulp-sass": "^2.3.2"
  },
  "scripts": {
    "build": "gulp clean & gulp build",
    "watch": "gulp watch",
    "start": "supervisor -w start.js,./server/*.js --debug start.js"
  }
}
